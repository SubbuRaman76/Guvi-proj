version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      NODE_ENV: production
    volumes:
      - .:/app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

